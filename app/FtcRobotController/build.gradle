apply plugin: 'com.android.application'

android {
    compileSdkVersion = 'Google Inc.:Google APIs:21'
    buildToolsVersion = '21.1.2'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 19
    }
}

dependencies {
    compile project(':lib:FtcCommon')
}
